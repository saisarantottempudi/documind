# !! Never commit real secrets — use Sealed Secrets or External Secrets Operator in production.
# This file holds base64-encoded placeholder values for local k3d use only.
# Generate real values: echo -n 'yourpassword' | base64
apiVersion: v1
kind: Secret
metadata:
  name: documind-secrets
  namespace: documind
type: Opaque
data:
  REDIS_PASSWORD: Y2hhbmdlbWU=   # changeme
